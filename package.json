{
  "name": "team-ensemble-cards",
  "version": "1.0.0",
  "description": "Static contact card generator for Team Ensemble",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "clean": "rm -rf dist",
    "dev": "concurrently \"nodemon --watch data --watch src --watch scripts --watch templates --ext js,yml,html,css --exec 'node scripts/build.js'\" \"http-server ./dist -c-1\""
  },
  "dependencies": {
    "handlebars": "^4.7.8",
    "js-yaml": "^4.1.1",
    "vcf": "^2.1.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "handlebars": "^4.7.8",
    "js-yaml": "^4.1.1",
    "nodemon": "^3.1.14",
    "serve": "^14.2.5",
    "vcf": "^2.1.2"
  },
  "engines": {
    "node": ">=18"
  }
}
